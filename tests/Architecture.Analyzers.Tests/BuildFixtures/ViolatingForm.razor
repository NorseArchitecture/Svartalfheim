@using Microsoft.AspNetCore.Components.Forms
@inherits Fixture.StubForm

<EditForm EditContext="EditContextFor(_request)" OnValidSubmit="Handle" />

@code {
	readonly object _request = new();
	void Handle(EditContext context) { }
}
