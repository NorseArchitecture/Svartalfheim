name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write
  packages: write
  security-events: write

jobs:
  release:
    uses: NorseArchitecture/.github/.github/workflows/release-nuget.yml@master
