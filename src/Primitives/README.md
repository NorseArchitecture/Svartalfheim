# Norse.Primitives

Norse forged primitives: the `Result<T>` discriminated union, closed parse-failure vocabulary, and hot-path scalar parsers for every boundary crossing into the Norse ecosystem from untrusted sources.

Part of the [Norse Architecture](https://github.com/NorseArchitecture) platform.
